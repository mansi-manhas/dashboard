import styled from "styled-components";

import { satisfactionRate } from "../../assets";

export const ProgressBarWrapper = styled.div`
    width: 230px;
    height: 230px;
    background: url(${satisfactionRate}) no-repeat;
    margin: auto;
`;